import { Container, Typography } from "@mui/material";

const Cart = () => {
  return (
    <Container sx={{ mt: 4 }}>
      <Typography variant="h4">Your Cart</Typography>
    </Container>
  );
};

export default Cart;
